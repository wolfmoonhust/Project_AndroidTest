<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="wolfdev01.com.demowithgit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.ProductDetailActivity"></activity>
        <activity android:name=".controller.ProductsActivity" />
        <activity android:name=".controller.ListViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.FinishActivity" />
        <activity android:name=".controller.SkillActivity" />
        <activity android:name=".controller.BaseActivity" />
        <activity android:name=".controller.LeagueActivity" />
        <activity android:name=".controller.WelcomeActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>